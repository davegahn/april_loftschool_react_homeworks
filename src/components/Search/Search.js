import React, { Component } from 'react';

export default class ShowPreview extends Component {
  render() {
    return (
      <div>
        <input type="text" value={{}} onChange={{}} placeholder="Название сериала" />
        <button onClick={{}}>Найти</button>
      </div>
    );
  }
}
